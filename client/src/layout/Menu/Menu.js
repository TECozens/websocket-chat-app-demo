import React from 'react'
import Rooms from '../../components/Rooms/Rooms'

const Menu = ({id}) => {
    return (
        <div>
            <Rooms id={id}/>
        </div>
    )
}

export default Menu
